server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    # This is crucial for React Router to work
    # Without this, refreshing /jobs/1 gives 404
    location / {
        try_files $uri $uri/ /index.html;
    }
}